import React, {createContext} from 'react';

const searchFoodContextData1 =createContext();

export default searchFoodContextData1;

//could have tsomethign like this for the charts and it changes on state 